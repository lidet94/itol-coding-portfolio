## Status Board
A simple React project that displays a ticket status board using reusable components, props, and props.children.

Each status panel shows a count, an image, and interactive buttons to update the count.

# Features:
- Three ticket status panels: Completed, In Progress and Failed
- Interactive counter buttons: Increase, Decrease and Reset
- Reusable Ticketinfo component
